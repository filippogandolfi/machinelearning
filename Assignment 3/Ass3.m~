%% Filippo Gandolfi 
% 4112879 - Robotics Engineering - 2019/2020
% Assignment 3
% Task
%         Task 1: Obtain a data set
%         Task 2: Build a kNN classifier
%         Task 3: Test the kNN classifiers
%
%% Task 2
%X = loadMNIST(0,[1,2,3]);
[data, labels] = loadMNIST(0);
% dataset = load('loadMNIST');
% [n,d] = size(dataset);
training_set = [data, labels]; % n x (d+1) matrix
% [ntr,dtr] = size(training_set); % size of training set
[test_set, test_labels] = loadMNIST(1); % m x d matrix 
% [nte,dte] = size(test_set); % size of test set
% k = ; % around integer for kNN
n = 1;
test_set = test_set(1:end/n,1:end);
training_set = 
% 
% %% checks
% if dtr = (dte-1)
%     disp('correct size of matrices'); %check the correct size of columns
% else
%     disp ('error on the matrices'); %error!
% end
% 
% if k>0 && k<=ntr
%     disp ('correct k');
% else
%     disp('wrong k');
% end
% 
% %% Classify the training set


    


